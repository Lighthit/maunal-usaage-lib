How to used GitHub 
{Installed git lib}
<!--firstime only-->
1.1 Go to folder that you want to upload it
1.2. $git init
1.3. $git add .    //" . " is mean all file in folder
1.4. $git commit -m "message"
1.5. $git remote add origin .git_Url  
1.6. $git pull origin main/master --rebase   //main and master is branch is spawn when i create new repositories      //first time only
1.7. $git push origin main/master         //first time only
<!--firstime only-->

{create new branch inside respositories}
2.1. $git checkout -b <new branch_name>
<!--if file is different from main/master branch -->
2.2. go_step 1.3 - 1.4
2.3. $git push origin <new branch_name>
<!--if file is same from main/master or some branch -->
2.4 git pull origin <branch name>


{most used command }
1. $git checkout <branch name>    //go to branch respositories
2. $git pull                      //pull code from git website if can not work used git pull origin <branch name>
3  $git add .                     // add file before push to website
4. $git commit -m "message here"  // write something  like update from ur code
5. $git push                      // upload ur code into website

extra 1 : $git reset --hard       // rollback ur code into last commit maybe in server or local
